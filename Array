#include <stdio.h>

int main() {
  // Create an array of 5 numbers
  int numbers[5] = {1, 2, 3, 4, 5};

  // Display the array
  for (int i = 0; i < 5; i++) {
    printf("%d ", numbers[i]);
  }
  printf("\n");

  return 0;
}
